<%= render "admin/header" %>

<div class = "container">
  <h3>商品詳細</h3>
  <div class= "row">
    <div class="col-lg-4">
      <%= attachment_image_tag @item, :image, size: "350x250", format: 'jpeg', fallback: "no_image.jpg" %>
    </div>
    <div class="col-lg-8">
      <p>商品名　　　　　　　<%= @item.name %></p>
      <p>商品説明　　　　　　<%= @item.introduction %></p>
      <p>ジャンル　　　　　　<%= @item.genre.name %></p>
      <p>税込価格(税抜価格)　<%= (@item.price*1.1).floor.to_s(:delimited) %>(<%= (@item.price).floor.to_s(:delimited) %>)円</p>
      <p>販売ステータス　　　<%= @item.is_active %></p>
      <%= link_to "編集する",edit_admin_item_path, class:"btn btn-success" %>
    </div>
  </div>

</div>